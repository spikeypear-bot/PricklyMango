# Plans
1) Config files in the form of yaml files (type of connections, ports to map to , directory name)   
2) BFF frontend using htmx (served on the same port)  
3) Script to read the config files and load handlers (using some load configs functions)
4) Concurrency handling with the functions
